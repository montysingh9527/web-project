## ✅ 一、基础类（必会）

1. **Vue 有哪几种常见的指令？**
   - `v-bind`, `v-model`, `v-if`, `v-for`, `v-on`, `v-show`, `v-slot` 等

------

## ✅ 二、进阶类（Vue 2 / Vue 3 通用）

1. **Vue 组件有哪几种通信方式？（props、$emit、$refs、provide/inject、eventBus、vuex、pinia）**
2. **什么是作用域插槽（scoped slot）？它解决了什么问题？**
3. **如何实现 keep-alive？有哪些生命周期？如何做缓存控制？**
4. **Vue 是如何实现数据双向绑定的？**
5. **Vue 中的事件修饰符和按键修饰符有哪些？**
6. **如何监听路由变化？如何设置路由懒加载？**
7. **路由跳转方式有哪些？如何实现路由守卫？**
8. **父组件更新会不会导致子组件重新渲染？为什么？**

------

## ✅ 三、Vue 3 特有题（组合式 API）

1. **Vue 3 中 setup 是什么时候执行的？其作用是什么？**
2. **Vue 3 的响应式是如何实现的？和 Vue 2 的响应式对比？**
3. **Vue 3 中 ref 和 reactive 的区别？如何访问 DOM？**
4. **watchEffect 和 watch 有什么区别？**
5. **如何自定义 Composition API？如何封装可复用逻辑？**
6. **Teleport 是什么？解决了什么问题？**
7. **Suspense 是做什么的？使用场景？**
8. **为什么说 Vue 3 更适合构建大型项目？（Tree shaking、Proxy、组合式 API）**

------

## ✅ 四、底层原理与手写题（高级）

1. **Vue 的响应式原理是怎样的？（Vue2 使用 Object.defineProperty，Vue3 使用 Proxy）**
2. **虚拟 DOM 的原理是什么？VNode 是什么？**
3. **diff 算法的核心思想是什么？Vue 是如何进行更新的？**
4. **手写一个简易版的响应式系统（模拟 Vue2 或 Vue3 的效果）**
5. **如何优化 Vue 项目的性能？**

------

## ✅ 五、实战场景题

1. **项目如何拆分模块？组件如何复用？**
2. **一个组件加载很慢怎么办？如何做性能优化？**
3. **如何实现组件的懒加载？**
4. **组件通信过于复杂怎么处理？Vuex 是否必须？**
5. **怎么在 Vue 项目中使用 TypeScript？遇到过哪些问题？**









#### Vue2和Vue3的区别

```
vue2: Object.defineProperty()
弊端:  1.对象直接新添加的属性或删除已有属性，界面不会自动更新，需要使用Vue.set()
	  2.直接通过下标替换元素或更新length，界面也不会自动更新

vue3: 代理new Proxy() / 反射Reflect.get()
proxy: 拦截对data任意属性的任意操作，包括属性值的读写，属性的添加，属性的删除等
Reflect: 动态对被代理对象的相应属性进行特定的操作
弊端:有兼容性问题的，IE完全不支持


vue2示例: 数组响应式会丢失
let vm = new Vue({
    data: {
        items: [1,2,3]
    }
})
vm.items[0] = 3; // item[0]将失去响应式
vm.length = 5; // vm.length将失去响应式

vue3示例: 数组响应式保持
const state = reactive({
  arr: [1, 2],
})
function increment() {
  state.arr[0]++; // 修改数组第一个元素：
  state.arr.length++; // 数组长度递增：
}
```

---

#### vue3生命周期

| 钩子函数      |      |
| ------------- | ---- |
| beforeCreate  |      |
| Created       |      |
| beforeMount   |      |
| mounted       |      |
| beforeUpdate  |      |
| updated       |      |
| beforeUnmount |      |
| unmounted     |      |

---

#### vue3组件怎么传值

- 父传子：使用 `props`
- 子传父：使用 `$emit`
- 跨层通信：`provide` / `inject`
- 全局共享状态Vuex/Pinia（推荐用 Pinia）

---

#### Vue 3 相比 Vue 2 的主要新特性

| 类别                   | Vue 3 新特性                       | Vue 2 状况                 | 简要说明                                            |
| ---------------------- | ---------------------------------- | -------------------------- | --------------------------------------------------- |
| 💡 响应式系统           | 使用 Proxy                         | 使用 Object.defineProperty | 更快更强，支持数组、Map、Set 等                     |
| Composition API        | ✅ 支持组合式 API (Composition API) | 选项式 API (Options API)   | 更灵活的逻辑复用（`setup()`、`ref`、`reactive` 等） |
| Fragment               | ✅ 支持多个根节点                   | ❌ 只能有一个根节点         | 组件可以返回多个元素                                |
| 🚀 性能提升             | 更快（体积更小、初始化更快）       | 相对慢                     | 虚拟 DOM 重写，树结构优化                           |
| Teleport               | ✅ 支持                             | ❌ 无                       | 将子组件 DOM 移动到其他位置（如模态框）             |
| Suspense               | ✅ 支持                             | ❌ 无                       | 原生异步组件加载状态                                |
| 更好的 TypeScript 支持 | ✅ 官方支持                         | ❌ 支持不佳                 | Vue 3 是为 TS 设计的                                |
| Tree-shaking 支持      | ✅ 更好                             | 一般                       | 按需引入，减小打包体积                              |
| 自定义渲染 API         | ✅ 支持                             | ❌ 无                       | 支持 SSR、自定义渲染器（如微信小程序）              |
| 更好的逻辑复用         | `setup()` + hooks                  | mixins                     | 不再混乱、命名冲突少                                |

---

#### vue3中setup有什么用？怎么执行的

> `setup()` 是**组件的核心入口函数**，Vue 3 组件的“初始化函数”，**在组件创建之前运行**，用于定义响应式数据、函数、生命周期等逻辑。

- **setup() 的执行时机**

  - 执行时机：**在组件创建前（beforeCreate 之前）执行**
  - 是组件的**第一次运行入口**
  - 此时组件实例 (`this`) **尚未创建**，所以 `setup` 中不能用 `this`

  <script setup> 等价于在普通 <script> 中手动写 setup() 函数，只不过写法更简单、更干净，且性能更好。

---

#### reactive() 与 ref()

````
reactive(): 仅适合对象类型的数据，例如对象、数组、Map、Set，不适用string、number、boolean等基础类型
ref(): 可以创建任意类型的响应式对象
使用ref()的好处：
-可以响应式的替换.value的值，并且替换后会保持响应式
-可以接收任意类型的值
-可以解构赋值，也可以直接使用.value的值作为函数的参数，这些都是保持响应式的
ref()会包装一个.value属性，因此在使用时可能会存在解包的情况：
-在模板中解包：如果作为顶层属性被访问时，可以不加.value，否则需要添加
-在响应式对象中解包：作为响应式对象的属性，.value会自动解包。需要注意，如果是数组、Map等集合类型的响应式对象中，访问元素时.value不会自动解包

总结:
Vue3使用Proxy实现响应式，无需像Vue2遍历原数据对象的每一个属性，可以更加高效的实现响应式。
Vue3中需要自己根据需要声明响应式对象，一般使用reactive()和ref()
reactive()多用于创建数组、对象等响应式对象，并且在之后的使用中不会发生替换、解构、属性值作为函数参数等情况
ref()可适用于任意类型，相当于是重新封装了一个对象，在替换、解构、属性值作为参数时都能保持响应式。
ref建议用于基础数据类型的响应式声明，如果是声明一个数组或对象，实际上会调用reactive将其转换为Proxy，并赋值给.value

reactive(): 示例
let state = reactive({count: 0})
let num = state.count; // num不具有响应式，其修改不会影响state.count
let {count} = state; // count也不具有响应式，其修改不会影响state.count

ref(): 示例
import {ref} from 'vue'
let num = ref(0) // 等价于{value: 0}
let str = ref('') // 等价于{value: ''}
let bool = ref(false) // 等价于{value: false}
````

---

#### computed 和 watch 的区别？使用场景？

> 想“展示计算值”：用 `computed`
>
> 想“监听一个或多个值变化去处理”：用 `watch`

**区别对比**

| 对比项         | `computed`                         | `watch`                                |
| -------------- | ---------------------------------- | -------------------------------------- |
| 用途           | 派生数据，通常用于模板或计算值展示 | 监听数据变化，执行副作用（API 调用等） |
| 是否需要返回值 | ✅ 有返回值（getter）               | ❌ 没有返回值（回调函数）               |
| 是否有缓存     | ✅ 有缓存（依赖不变不重新计算）     | ❌ 无缓存，每次变化都执行               |
| 响应式特点     | 自动跟踪依赖，变化时重新计算       | 需要手动指定要观察的变量               |
| 用于模板展示   | ✅ 常用于模板中                     | ⚠️ 通常不用于模板                       |
| 参数形式       | `computed(() => ...)`              | `watch(source, callback)`              |

----

#### Vue 中 key 的作用？为什么建议在列表渲染中加 key？

> **主要用于列表渲染（v-for）时提高性能和准确性**

> 通过sameVnode方法对vnode元素进行比较，如果每个元素都进行替换，将大大影响性能。

- **如果设置了key是index的情况**
  - 列表每一项index的值从0、1、2、3，当我们在头部插入一个节点，是不是插入的节点就变成了0，原来的0、1、2、3都加一位，这样标签都不能服用，全部都需要重新渲染，影响性能。
- **如果设置了固定key**
  - 下面我们来把key设置成唯一的值，且是固定的值，当我们在头部插入一个节点，这个节点的key没有一样的，我们就创建一个，插入到头部后面的节点，是不是sel没有变，标签没有变，key值也没有变，是不是节点全部都可以复用，只是把位置挪动下，实际上就只创建了一个元素，这样就可以大大加快渲染速度

---

#### v-model 的原理是什么？如何自定义组件的 v-model？

> `v-model` 本质是：`:value`（绑定值） + `@input`（监听变化） 的简写形式。
> 在 Vue 3 中，它是通过 `modelValue` + `update:modelValue` 实现的。

- **v-model原理**

  ```vue
  <input v-model="name" />
  
  等价于：
  <input :value="name" @input="name = $event.target.value" />
  ```

- **Vue 3 中 v-model 在自定义组件中的工作机制**

  ```vue
  ===默认绑定 modelValue + 触发 update:modelValue：===
  <MyInput v-model="name" />
  
  等价于：
  <MyInput :modelValue="name" @update:modelValue="val => name = val" />
  ```

  **Vue 2 和 Vue 3 中 v-model 的区别：**

  | 比较项       | Vue 2       | Vue 3                |
  | ------------ | ----------- | -------------------- |
  | 默认 prop    | `value`     | `modelValue`         |
  | 默认事件     | `input`     | `update:modelValue`  |
  | 多个 v-model | ❌ 不支持    | ✅ 支持（命名）       |
  | 实现方式     | options API | 推荐 Composition API |

---

#### v-if 和 v-show 的区别？

> `v-if` 是 **条件渲染**（真正地添加/移除 DOM）
> `v-show` 是 **条件显示**（切换 `display: none`）

| 对比项           | `v-if`                              | `v-show`                                      |
| ---------------- | ----------------------------------- | --------------------------------------------- |
| 渲染机制         | 不满足条件时，**不会渲染 DOM 元素** | DOM 元素始终存在，只是用 `display: none` 隐藏 |
| 初始渲染开销     | 大（每次都创建/销毁 DOM）           | 小（一次渲染，后续仅样式切换）                |
| 切换开销         | 较大（添加/销毁 DOM）               | 较小（仅切换样式）                            |
| 是否参与过渡动画 | ✅ 支持 `v-if` 过渡动画              | ❌ 不支持 `v-show` 的 enter/leave 动画         |
| 使用场景         | 条件**偶发性**变化                  | 频繁显示/隐藏切换                             |

---









---

#### props的默认值是对象Object或数组Array,默认值为什么是一个函数?

```
当默认值是对象或者数组时,必须从一个工厂函数返回.
默认值不是必须的,当需要设置默认值时使用.

default: () => []
default: () => ({})
参考:https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Functions/Arrow_functions#%E8%BF%94%E5%9B%9E%E5%AF%B9%E8%B1%A1%E5%AD%97%E9%9D%A2%E9%87%8F
```

---

#### vue3源码解析

- **ref**

![Alt text](./img/ref.png)

- **reactive**

![Alt text](./img/reactive.png)

---

